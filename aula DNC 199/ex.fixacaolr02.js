//Escreva um laço while que imprime os números de 1 a 5 no console

var c = 1; // Inicializa a variável

while (c <= 5) { // Define a condição para continuar o loop
    console.log(c); // Exibe o valor de i no console
    c++; // Incrementa i a cada repetição
}